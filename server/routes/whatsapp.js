import express from 'express';
// import serverInfo from '../../server.js';

const router = express.Router();

// router.get('/validate-conn-whatsapp', async (req, res) => {
//   console.log(serverInfo.isAuthenticated);
//   res.json(serverInfo.isAuthenticated);
// });

export default router;
